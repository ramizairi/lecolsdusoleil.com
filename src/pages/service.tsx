import HomePageShell from "@/components/home/HomePageShell";

const ServicePage = () => {
  return <HomePageShell canonicalPath="/service" logoMode="always" activeKey="services" />;
};

export default ServicePage;
