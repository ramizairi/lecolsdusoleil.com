import HomePageShell from "@/components/home/HomePageShell";

const HomePage = () => {
  return (
    <HomePageShell
      canonicalPath="/"
      logoMode="scroll"
      activeKey="accueil"
      heroTopSpacing="compact"
    />
  );
};

export default HomePage;
